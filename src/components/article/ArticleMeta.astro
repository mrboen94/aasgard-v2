---
import "./article.css";

interface Props {
  eyebrow?: string;
  date: Date;
  updated?: Date;
  tags?: string[];
  section?: string;
}

const { eyebrow, date, updated, tags = [], section } = Astro.props;
const dateLabel = date.toLocaleDateString("en", {
  year: "numeric",
  month: "short",
  day: "numeric"
});
---

<div class="article-meta meta">
  {eyebrow && <span class="eyebrow" data-pagefind-meta="eyebrow">{eyebrow}</span>}
  {section && <span data-pagefind-meta="section">{section}</span>}
  <time datetime={date.toISOString()} data-pagefind-meta="date">{dateLabel}</time>
  {updated && <span>Updated {updated.toLocaleDateString("en")}</span>}
  {tags.length > 0 && (
    <ul aria-label="Tags">
      {tags.map((tag) => <li data-pagefind-meta="tags">{tag}</li>)}
    </ul>
  )}
</div>
