---
import {
  articleProseClass,
  articleProseWideClass
} from "../../lib/styles/classes";

interface Props {
  wide?: boolean;
}

const { wide = false } = Astro.props;
---

<div class:list={[articleProseClass, wide && articleProseWideClass]}>
  <slot />
</div>
