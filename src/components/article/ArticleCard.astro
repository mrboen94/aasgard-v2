---
import "./article.css";
import { articleUrl } from "../../lib/content/urls";
import type { ArticleEntry } from "../../lib/content/collections";
import ArticleMeta from "./ArticleMeta.astro";

interface Props {
  entry: ArticleEntry;
}

const { entry } = Astro.props;
---

<article class="article-card listing-card surface">
  <a class="article-card__link listing-card__link" href={articleUrl(entry.data.slug)}>
    <span class="listing-card__eyebrow">{entry.data.template}</span>
    <h3>{entry.data.title}</h3>
    <p class="listing-card__copy">{entry.data.description}</p>
  </a>
  <ArticleMeta date={entry.data.date} tags={entry.data.tags} />
</article>
