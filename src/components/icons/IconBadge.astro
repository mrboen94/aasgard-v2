---
import "./icons.css";
import { Icon } from "astro-icon/components";

interface Props {
  name: string;
  label: string;
}

const { name, label } = Astro.props;
---

<span class="icon-badge" title={label}>
  <Icon name={name} aria-hidden="true" />
  <span class="visually-hidden">{label}</span>
</span>
