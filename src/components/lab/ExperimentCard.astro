---
import "./experiment-card.css";
import type { ExperimentEntry } from "../../lib/content/collections";
import { labUrl } from "../../lib/content/urls";
import TechIconList from "../icons/TechIconList.astro";

interface Props {
  entry: ExperimentEntry;
}

const { entry } = Astro.props;
---

<article class="experiment-card listing-card surface">
  <a class="listing-card__link" href={labUrl(entry.data.slug)}>
    <span class="listing-card__eyebrow">{entry.data.status}</span>
    <h3>{entry.data.title}</h3>
    <p class="listing-card__copy">{entry.data.description}</p>
  </a>
  <TechIconList technologies={entry.data.technologies} />
</article>
