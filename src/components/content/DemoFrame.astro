---
import Panel from "../ui/Panel.astro";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Panel as="section" class="overflow-hidden" margin="lg">
  {title && <h2 class="mb-3 text-base">{title}</h2>}
  <slot />
</Panel>
