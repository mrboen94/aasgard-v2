---
import "./content.css";

interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<aside class="content-aside">
  {title && <p class="content-aside__title">{title}</p>}
  <slot />
</aside>
