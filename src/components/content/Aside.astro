---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<aside class="my-6 pl-4 text-caption text-muted">
  {title && <p class="mt-0 mb-2 font-accent font-bold text-foreground">{title}</p>}
  <slot />
</aside>
