---
import "./content.css";

interface Props {
  href: string;
  title: string;
  description?: string;
}

const { href, title, description } = Astro.props;
---

<a class="link-card surface" href={href}>
  <span>{title}</span>
  {description && <p>{description}</p>}
</a>
