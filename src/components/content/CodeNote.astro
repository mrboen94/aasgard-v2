---
import Panel from "../ui/panels/Panel.astro";

interface Props {
  title?: string;
}

const { title = "Code note" } = Astro.props;
---

<Panel as="aside" margin="sm" variant="code">
  <p class="mt-0 mb-2 font-accent text-muted">{title}</p>
  <slot />
</Panel>
