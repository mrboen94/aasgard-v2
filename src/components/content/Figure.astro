---
import { captionClass } from "../../lib/styles/classes";

interface Props {
  caption?: string;
}

const { caption } = Astro.props;
---

<figure class="my-8">
  <slot />
  {caption && <figcaption class={captionClass}>{caption}</figcaption>}
</figure>
