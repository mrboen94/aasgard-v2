---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...attrs } = Astro.props as Props;
---

<figcaption class:list={["mt-2 font-accent text-small text-muted", className]} {...attrs}>
  <slot />
</figcaption>
