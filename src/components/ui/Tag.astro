---
interface Props {
  as?: string;
  class?: string;
  [key: string]: any;
}

const { as = "li", class: className, ...attrs } = Astro.props as Props;
const Tag = as;
---

<Tag class:list={["rounded-full border border-border px-2", className]} {...attrs}>
  <slot />
</Tag>
